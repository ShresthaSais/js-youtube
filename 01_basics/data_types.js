// primitive

// 7 types : String, Number, Boolean, null, undefined, Symbol, BigInt

// Reference (non primitive)

// Array, Objects (master objects, master web events and browser events then you'll have mastered JS - Chai aur code), Functions

let myObj = {
    name : "sais",
    age : 25,


}

const myFunction = function () {
    console.log("Hello World");
    
}

// all non-primitive data types when returned using typeOf , it'll return object.

